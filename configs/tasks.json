{
  "demo_add": {
    "description": "Demo task for Week-1: ensure add(a,b) works and gates pass.",
    "entrypoints": [
      "src/example_pkg/math_utils.py"
    ]
  },
  "demo_add_weak_tests": {
    "description": "Demo task with intentionally weak tests to trigger mutation-strengthen loop",
    "entrypoints": [
      "src/example_pkg/math_utils.py"
    ]
  },
  "generate_email_validator": {
    "description": "CodeAgent GENERATE mode: Create an email validation function",
    "entrypoints": [
      "src/example_pkg/validators.py"
    ],
    "mode": "generate",
    "feature_description": "Create a function validate_email(email: str) -> bool that validates email addresses"
  }
}
